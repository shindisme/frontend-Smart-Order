# React + Vite

Smart Order (React + Express + Mysql + Nodejs)

# Firt install

cd backend and npm install
cd frontend and npm install

# Run project

cd backend and npm run dev
cd frontend and npm run dev
